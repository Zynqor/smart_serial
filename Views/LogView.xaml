<UserControl x:Class="SerialProtocolAssistant.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="800">
    <ui:Card Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="日志" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <ListBox x:Name="LogListBox"
                     Grid.Row="1"
                     ItemsSource="{Binding LogMessages}"
                     MinHeight="100"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     SelectionMode="Extended"
                     PreviewMouseWheel="LogListBox_PreviewMouseWheel">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5,2"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock x:Name="LogTextBlock" Text="{Binding}" FontFamily="Consolas" FontSize="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=LogFontSize}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </ui:Card>
</UserControl>
