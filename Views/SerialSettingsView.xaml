<UserControl x:Class="SerialProtocolAssistant.Views.SerialSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <ui:Card Margin="5">
        <StackPanel>
            <TextBlock Text="串口设置" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="端口:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="0" Grid.Column="1" Margin="5"
                          ItemsSource="{Binding AvailablePorts}"
                          SelectedItem="{Binding SelectedPort}"/>

                <TextBlock Text="波特率:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="5"
                          ItemsSource="{Binding BaudRates}"
                          SelectedItem="{Binding SelectedBaudRate}"/>

                <TextBlock Text="数据位:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Margin="5"
                          ItemsSource="{Binding DataBitsOptions}"
                          SelectedItem="{Binding SelectedDataBits}"/>

                <TextBlock Text="校验位:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="3" Grid.Column="1" Margin="5"
                          ItemsSource="{Binding ParityOptions}"
                          SelectedItem="{Binding SelectedParity}"/>

                <TextBlock Text="停止位:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox Grid.Row="4" Grid.Column="1" Margin="5"
                          ItemsSource="{Binding StopBitsOptions}"
                          SelectedItem="{Binding SelectedStopBits}"/>

                <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,0">
                    <ui:Button Content="刷新端口" Command="{Binding RefreshPortsCommand}" Margin="0,0,5,0"/>
                    <ui:Button Content="{Binding IsConnected, Converter={StaticResource BoolToConnectionTextConverter}}"
                               Command="{Binding ToggleConnectionCommand}"
                               Appearance="{Binding IsConnected, Converter={StaticResource BoolToAppearanceConverter}}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ui:Card>
</UserControl>
